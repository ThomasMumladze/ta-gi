import React from "react";
import Contacts from "./Contacts";

const Home = () => {
    return (
        <React.Fragment>
            <Contacts />
        </React.Fragment>
    );
};

export default Home;
